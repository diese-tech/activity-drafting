[build]
builder = "NIXPACKS"
buildCommand = "npm install -g pnpm && pnpm install && pnpm --filter client build && pnpm --filter server build"

[deploy]
startCommand = "NODE_ENV=production node packages/server/dist/app.js"
